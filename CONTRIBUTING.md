# Contributing to Aether
We follow strict standards to ensure scalability and security.

### 🛠 Setup
1. Install **Go 1.24+** and **Docker**.
2. Clone: `git clone https://github.com/shaa2020/aether-core.git`
3. Run: `go run cmd/server/main.go`

### 🤝 Standards
* **Commits**: Use prefixes like `feat:`, `fix:`, or `docs:`.
* **Tests**: All new logic requires at least 80% test coverage.
* **Security**: Never commit plain-text credentials.
